# 2026-03-13 工作日誌

## 今日工作摘要

### 1. 銷貨匯入腳本檢查
- **問題**：銷貨匯入腳本解析到 0 筆記錄
- **原因**：匯出的 CSV 格式為「客戶銷貨明細表」，與腳本預期的「業務員銷貨明細」格式不符
- **解決**：請使用者重新上傳正確格式的檔案，成功匯入 13 筆記錄

### 2. 外勤服務表登入問題修復
- **問題**：5628 梁仁佑登入時，業務人員姓名不會出現，打的資料也不會出現
- **原因**：
  - 使用者將瀏覽器分頁一直停在「外勤服務表」頁面
  - 隔天開啟時頁面直接從記憶體還原，沒有重新載入
  - `localStorage` 已過期（晚上9點自動登出）
  - `DOMContentLoaded` 事件已在昨天觸發過，不會再執行
- **修復**：在 `service_record.html` 新增 `window.onLoginSuccess` 回調函數
  - 當 `base.html` 完成登入後會自動觸發此回調
  - 正確設定業務人員名稱並重新載入紀錄

### 3. 服務紀錄資料修正
- 將「振良行」的業務人員從空白更新為「梁仁佑」

### 4. 登入後自動轉跳首頁
- **修改**：`base.html` 登入成功處理邏輯
- **行為**：登入成功後，如果不是在首頁，自動轉跳至首頁 (`/`)
- **效益**：統一進入點，避免頁面狀態不一致的問題

## 修改檔案

| 檔案 | 變更內容 |
|------|----------|
| `dashboard-site/service_record.html` | 新增 `onLoginSuccess` 回調 |
| `dashboard-site/base.html` | 登入成功後自動轉跳首頁 |

## Git 提交

```
bd917db Fix service_record.html: add onLoginSuccess callback to set salesperson name after login
27f58cb Add auto-redirect to home page after login success
```

## 技術筆記

### 登入流程時序問題
- `base.html` 和子頁面的 `DOMContentLoaded` 是並行的
- 子頁面不能依賴 `DOMContentLoaded` 時讀取 `localStorage`
- 應使用 `window.onLoginSuccess` 回調機制，確保登入完成後才執行頁面初始化

### 自動轉跳邏輯
```javascript
if (window.location.pathname !== '/' && window.location.pathname !== '/index.html') {
    window.location.href = '/';
}
```

### 5. 登入轉跳邏輯修正
- **問題**：登入後自動轉跳首頁的功能，導致從首頁點選單切換頁面時也會跳回首頁
- **原因**：轉跳邏輯放在 `requireLogin` 的 Promise resolve 中，每次檢查登入狀態都會執行
- **修復**：
  - `auth_ui.js`：新增 `_justLoggedIn` 標記區分「剛登入」和「已登入」
  - `base.html`：只在 `_justLoggedIn = true` 時才轉跳首頁
- **行為**：
  - 從書籤/分頁還原登入 → 轉跳首頁 ✅
  - 從首頁點選單切換頁面 → 不轉跳 ✅

### 6. 網頁伺服器維護
- **Port 3000** (電腦舖營運系統)：重啟服務，確認正常運作
- **Port 8000** (電腦舖官網)：重啟服務，確認正常運作
- **Cloudflare Tunnel**：檢查狀態，憑證問題待處理

### 7. 系統命名確認
- **正式名稱**：電腦舖營運系統（原 Dashboard Site）
- **對外官網**：電腦舖官網（Port 8000）
- 待下次更新統一修改程式碼中的顯示名稱

## 修改檔案

| 檔案 | 變更內容 |
|------|----------|
| `dashboard-site/service_record.html` | 新增 `onLoginSuccess` 回調 |
| `dashboard-site/base.html` | 登入成功後自動轉跳首頁（含修正） |
| `dashboard-site/shared/auth_ui.js` | 新增 `_justLoggedIn` 標記 |

## Git 提交

```
bd917db Fix service_record.html: add onLoginSuccess callback to set salesperson name after login
27f58cb Add auto-redirect to home page after login success
e1e218c Fix login redirect: only redirect to home page when user just logged in, not on every page load
```

## 技術筆記

### 登入流程時序問題
- `base.html` 和子頁面的 `DOMContentLoaded` 是並行的
- 子頁面不能依賴 `DOMContentLoaded` 時讀取 `localStorage`
- 應使用 `window.onLoginSuccess` 回調機制，確保登入完成後才執行頁面初始化

### 自動轉跳邏輯
```javascript
// 只有「剛登入」且不在首頁時，才轉跳至首頁
if (user._justLoggedIn && window.location.pathname !== '/' && window.location.pathname !== '/index.html') {
    window.location.href = '/';
}
```

### 8. 推薦備貨商品功能開發
- **功能概述**：讓老闆/會計預設建議備貨商品，門市/業務可直接點選數量送出需求
- **資料表設計**：
  - `recommended_categories` - 商品分類
  - `recommended_products` - 推薦商品（欄位對應 needs 表）
- **頁面開發**：
  - 後台管理頁面 (`/admin/recommended_products.html`)
  - 前台選購頁面 (`/recommended_products.html`)
- **API 開發**：分類管理、商品管理、送出備貨需求
- **權限控制**：僅老闆/會計可見（未來開放給門市/業務）

### 9. 推薦備貨商品功能修正
- **產品搜尋功能**：整合至產品名稱欄位，可手動輸入或搜尋帶入
- **欄位順序調整**：產品編號 → 產品名稱
- **API 修正**：使用正確的 `products` 資料表
- **白皮書更新**：新增第11章詳細說明

## 修改檔案

| 檔案 | 變更內容 |
|------|----------|
| `dashboard-site/service_record.html` | 新增 `onLoginSuccess` 回調 |
| `dashboard-site/base.html` | 登入成功後自動轉跳首頁（含修正）、新增選單項目 |
| `dashboard-site/shared/auth_ui.js` | 新增 `_justLoggedIn` 標記 |
| `dashboard-site/admin/recommended_products.html` | 後台管理頁面（新增） |
| `dashboard-site/recommended_products.html` | 前台選購頁面（新增） |
| `dashboard-site/app.py` | 推薦商品 API（新增） |
| `docs/whitepaper_recommended_products.md` | 功能白皮書（新增） |
| `ERP_Whitepaper.md` | 新增第11章推薦備貨商品模組 |

## Git 提交

```
bd917db Fix service_record.html: add onLoginSuccess callback to set salesperson name after login
27f58cb Add auto-redirect to home page after login success
e1e218c Fix login redirect: only redirect to home page when user just logged in, not on every page load
a9c9025 Update memory log for 2026-03-13
d5c5ef3 Add recommended products feature: backend admin, frontend page, APIs, and menu integration
3960130 Update recommended products: change model_no to product_code, add product search with auto-fill
de9e317 Align recommended_products schema with needs table: item_name, product_code, quantity, requester, department
bf7b17f Add whitepaper for recommended products feature
755c1b5 Update ERP whitepaper: add recommended products feature module v1.1
89ea893 Fix product search: use correct table name 'products', reorder fields (code before name), remove redundant item_name field
06fd173 Merge product search into item_name field: single input for manual entry or search
9bc016b Reorder fields: product code above product name
4b1de27 Update whitepaper: reflect field order change and merged product name input
```

## 技術筆記

### 登入流程時序問題
- `base.html` 和子頁面的 `DOMContentLoaded` 是並行的
- 子頁面不能依賴 `DOMContentLoaded` 時讀取 `localStorage`
- 應使用 `window.onLoginSuccess` 回調機制，確保登入完成後才執行頁面初始化

### 自動轉跳邏輯
```javascript
// 只有「剛登入」且不在首頁時，才轉跳至首頁
if (user._justLoggedIn && window.location.pathname !== '/' && window.location.pathname !== '/index.html') {
    window.location.href = '/';
}
```

### 推薦備貨商品資料表設計原則
- 欄位直接對應 `needs` 資料表，送出時無需轉換
- `item_name` - 產品名稱（可手動輸入或從主檔搜尋帶入）
- `product_code` - 產品編號（唯讀，搜尋帶入）
- `quantity` - 最小備貨量

### 10. 銷售獎金計算功能開發
- **功能概述**：設定特定商品在特定時間銷售的額外獎金規則，自動計算並提供確認流程
- **資料表設計**：
  - `bonus_rules` - 獎金規則（商品、時間、獎金類型、數值）
  - `bonus_results` - 計算結果（人員、商品、數量、銷售額、獎金）
  - `bonus_payments` - 發放記錄
- **頁面開發**：
  - 後台規則管理 (`/admin/bonus_rules.html`)
  - 獎金報表 (`/admin/bonus_report.html`)
  - 個人查詢 (`/bonus_personal.html`)
- **API 開發**：規則管理、獎金計算、結果查詢、確認/批次確認
- **權限控制**：僅老闆/會計可見（未來開放給員工）
- **功能特點**：
  - 產品搜尋帶入（編號+名稱）
  - 固定金額或百分比獎金
  - 最小數量門檻
  - 適用範圍設定（全部/特定人員）
  - 自動計算與手動確認流程
  - CSV 匯出報表

### 11. 銷售獎金功能修正
- **產品搜尋整合**：與推薦備貨商品一致，產品名稱欄位可搜尋帶入
- **欄位順序**：產品編號 → 產品名稱
- **計算邏輯**：依時間區間、商品匹配、數量門檻自動計算
- **白皮書更新**：新增第12章，合併至 `ERP_Whitepaper.md`

## 修改檔案

| 檔案 | 變更內容 |
|------|----------|
| `dashboard-site/service_record.html` | 新增 `onLoginSuccess` 回調 |
| `dashboard-site/base.html` | 登入轉跳修正、新增選單項目（備貨推薦、銷售獎金） |
| `dashboard-site/shared/auth_ui.js` | 新增 `_justLoggedIn` 標記 |
| `dashboard-site/admin/recommended_products.html` | 後台管理頁面 |
| `dashboard-site/recommended_products.html` | 前台選購頁面 |
| `dashboard-site/admin/bonus_rules.html` | 獎金規則管理頁面（新增） |
| `dashboard-site/admin/bonus_report.html` | 獎金報表頁面（新增） |
| `dashboard-site/bonus_personal.html` | 個人獎金查詢頁面（新增） |
| `dashboard-site/app.py` | 推薦商品 API、獎金 API（新增） |
| `docs/whitepaper_recommended_products.md` | 推薦備貨商品白皮書 |
| `docs/whitepaper_bonus.md` | 銷售獎金白皮書 |
| `ERP_Whitepaper.md` | 新增第11章、第12章 |

## Git 提交（今日總計 20+ 筆）

**主要提交**：
```
# 登入與外勤服務表修復
bd917db Fix service_record.html: add onLoginSuccess callback
27f58cb Add auto-redirect to home page after login success
e1e218c Fix login redirect: only redirect when user just logged in

# 推薦備貨商品功能
d5c5ef3 Add recommended products feature: backend admin, frontend page, APIs
3960130 Update recommended products: change model_no to product_code
de9e317 Align schema with needs table
755c1b5 Update ERP whitepaper: add recommended products module v1.1

# 銷售獎金功能
d7853e5 Add sales bonus feature: rules management, report page, personal query
aa9d57d Add sales bonus feature whitepaper
d6a7ba6 Merge sales bonus whitepaper into ERP_Whitepaper.md
```

## 今日完成總覽

| 功能模組 | 狀態 | 頁面 |
|---------|------|------|
| 外勤服務表登入修復 | ✅ | `/service_record` |
| 推薦備貨商品 | ✅ | `/admin/recommended_products`, `/recommended_products` |
| 銷售獎金計算 | ✅ | `/admin/bonus_rules`, `/admin/bonus_report`, `/bonus_personal` |

## 技術筆記

### 登入流程時序問題
- `base.html` 和子頁面的 `DOMContentLoaded` 是並行的
- 應使用 `window.onLoginSuccess` 回調機制，確保登入完成後才執行頁面初始化

### 自動轉跳邏輯
```javascript
// 只有「剛登入」且不在首頁時，才轉跳至首頁
if (user._justLoggedIn && window.location.pathname !== '/' && window.location.pathname !== '/index.html') {
    window.location.href = '/';
}
```

### 產品搜尋整合模式
- 產品名稱欄位：可手動輸入或輸入關鍵字搜尋
- 點擊搜尋結果：自動帶入產品編號（唯讀）+ 產品名稱
- 應用於：推薦備貨商品、銷售獎金規則

### 獎金計算邏輯
```python
def calculate_bonus(sale, rule):
    # 檢查時間區間
    if not (rule.start_date <= sale.date <= rule.end_date):
        return 0
    # 檢查商品匹配（編號或名稱模糊匹配）
    # 檢查數量門檻
    # 計算：固定金額 × 數量 或 銷售額 × 百分比
```

---
_記錄時間：2026-03-13 18:30_
_記錄者：Yvonne_
