# 2026-03-23 工作記錄

## 今日完成項目

### 1. ERP 營運系統權限調整

**需求來源：** Alan (老闆)

**修改內容：**
- 「推薦備貨商品」頁面開放給所有人
- 「我的獎金」頁面開放給所有人，並將文字改為「銷售獎金」

**修改檔案：**
1. `dashboard-site/base.html`
   - 行 723：推薦備貨商品 roles 從 `['老闆', '會計']` 改為 `['老闆', '會計', '門市工程師', '業務人員', '業務部主管', '門市部主管']`
   - 行 734：我的獎金 label 從「我的獎金」改為「銷售獎金」，roles 同步開放給所有人

2. `dashboard-site/bonus_personal.html`
   - 頁面標題從「我的獎金查詢」改為「銷售獎金查詢」
   - block title 和 page_title 同步更新

**資料安全機制：**
- 銷售獎金 API (`/api/bonus-results`) 仍依據 `salesperson_id` 參數過濾資料
- 每個使用者只能查詢到自己的獎金記錄，無法查看他人資料

**部署：**
- Gunicorn 已重啟 (PID: 1390)
- 修改已生效

---

## 系統狀態

- ERP 營運系統：正常運行 (Port 3000)
- Gunicorn：正常 (PID: 1390)
- 資料庫：連線正常

### 2. 版本號更新

**修改檔案：**
- `dashboard-site/index.html`
  - 版本號從 `V2.0.2` 更新為 `V2.0.3`

### 3. 稽核儀表頁面修復

**問題描述：**
- 稽核儀表頁面無法看到內容
- 左側選單列沒有顯示

**問題原因：**
- `admin.html` 中有重複的 `showToast` 函數定義（行 805 和行 2246），可能導致 JavaScript 錯誤
- `loadAuditSummary` 和 `showAuditDetail` 函數缺少防護檢查，可能在登入完成前被呼叫

**修復內容：**
1. `dashboard-site/admin.html`
   - 刪除重複的 `showToast` 函數定義（行 2246）
   - 為 `loadAuditSummary` 函數添加防護檢查：
     ```javascript
     if (!currentAdmin || !currentAdmin.name) {
         console.warn('[Admin] loadAuditSummary called before login');
         return;
     }
     ```
   - 為 `showAuditDetail` 函數添加防護檢查

**部署：**
- Gunicorn 已重啟 (PID: 1771)
- 修改已生效

---

## 系統狀態

- ERP 營運系統：正常運行 (Port 3000)
- Gunicorn：正常 (PID: 1771)
- 資料庫：連線正常

### 4. Edge TTS 語音合成工具安裝與測試

**工具介紹：**
- edge-tts 是一個基於 Microsoft Edge 線上 TTS 服務的命令列工具
- 支援多種語言和語音，包括中文（台灣、中國、香港）
- 可調整語速、音調、音量
- 支援生成字幕檔案（VTT 格式）

**安裝過程：**
1. 安裝 pipx：`brew install pipx`
2. 安裝 edge-tts：`pipx install edge-tts`
3. 安裝版本：7.2.8

**測試結果：**

| 語音 | 語言 | 檔案大小 | 狀態 |
|------|------|----------|------|
| zh-TW-HsiaoChenNeural | 中文（台灣）| 40K | ✅ 正常 |
| zh-CN-XiaoxiaoNeural | 中文（中國）| 31K | ✅ 正常 |
| zh-HK-HiuGaaiNeural | 中文（香港）| 38K | ✅ 正常 |

**可用指令：**
```bash
# 列出所有語音
edge-tts --list-voices

# 基本語音合成
edge-tts --voice zh-TW-HsiaoChenNeural --text "你好" --write-media output.mp3

# 調整語速和音調
edge-tts --voice zh-TW-HsiaoChenNeural --rate "+20%" --pitch "+10Hz" --text "你好" --write-media output.mp3

# 生成字幕
edge-tts --voice zh-TW-HsiaoChenNeural --text "你好" --write-media output.mp3 --write-subtitles output.vtt

# 直接播放（使用 mpv）
edge-playback --voice zh-TW-HsiaoChenNeural --text "你好"
```

**推薦中文語音：**
- **zh-TW-HsiaoChenNeural** (曉辰) - 台灣女生，溫暖自然
- **zh-TW-HsiaoYuNeural** (曉雨) - 台灣女生，活潑
- **zh-TW-YunJheNeural** (雲哲) - 台灣男生
- **zh-CN-XiaoxiaoNeural** (曉曉) - 中國女生，新聞播報風格
- **zh-CN-YunxiNeural** (雲希) - 中國男生，活潑陽光

---

## 系統狀態

- ERP 營運系統：正常運行 (Port 3000)
- Gunicorn：正常 (PID: 1771)
- 資料庫：連線正常
- Edge TTS：已安裝，可正常使用

### 5. 移除稽核儀表功能

**決策：** 暫時移除稽核儀表頁面與相關 API，未來有需要時重新開發

**刪除內容：**
1. `dashboard-site/admin.html` - 已備份為 `admin.html.bak.20260323223026`
2. `dashboard-site/base.html` - 移除「稽核儀表」和「操作日誌」選單項目
3. `dashboard-site/app.py` - 移除以下 API：
   - `/api/admin/audit/summary` (稽核摘要)
   - `/api/admin/audit/detail` (稽核明細)
   - `/api/admin/audit-log` (操作日誌查詢)

**保留功能：**
- `admin_audit_log` 資料表（系統內部記錄用）
- `/admin/health` (系統健康檢查)
- `/admin/bonus_rules` (獎金規則管理)
- `/admin/bonus_report` (獎金報表)
- `/admin/announcement_management` (公告管理)
- `/admin/recommended_products` (推薦備貨商品管理)

**部署：**
- Gunicorn 已重啟 (PID: 10487)
- 修改已生效

### 6. 更新 ERP 白皮書 v3.3

**更新檔案：** `dashboard-site/ERP_Whitepaper_v3.0.md`

**更新內容：**
- 版本號：v3.2 → v3.3
- 最後更新日期：2026-03-23
- 移除頁面清單中的「系統管理 (admin.html)」
- 版本歷史新增 v3.3 更新說明

**GitHub Push：**
- 提交哈希：`3e36740`
- 更新時間：2026-03-23 22:44

---

## 今日工作總結

| 時間 | 項目 | 狀態 |
|------|------|------|
| 15:34 | ERP 權限調整 | ✅ 完成 |
| 15:40 | 版本號更新 2.0.3 | ✅ 完成 |
| 15:42 | 稽核儀表修復 | ✅ 完成 |
| 18:17 | Edge TTS 安裝 | ✅ 完成 |
| 18:25 | 預設語音設定 | ✅ 完成 |
| 22:27 | 移除稽核儀表 | ✅ 完成 |
| 22:44 | 白皮書更新 v3.3 | ✅ 完成 |

---

## 系統狀態

- ERP 營運系統：正常運行 (Port 3000)
- Gunicorn：正常 (PID: 10487)
- 資料庫：連線正常
- Edge TTS：已安裝，可正常使用

---

_記錄時間：2026-03-23 22:45_
_記錄者：Yvonne_

晚安，Alan！🌙
