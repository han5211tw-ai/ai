# 工作記錄 - 2026-03-27

## 今日完成項目

### 1. LINE 官方帳號回覆表系統 ✅

**任務**: 建立 LINE 官方帳號回覆表功能，取代雲端文件填寫

**完成內容**:
- 資料表 `line_replies` 建立（SQLite）
- 6組 API 端點：
  - `GET /api/line-replies` - 查詢列表（分頁、篩選、搜尋）
  - `POST /api/line-replies` - 新增記錄
  - `GET /api/line-replies/<id>` - 取得單筆
  - `PUT /api/line-replies/<id>` - 更新記錄
  - `DELETE /api/line-replies/<id>` - 刪除記錄
  - `GET /api/line-replies/stats/*` - 門市/人員統計
- 前端頁面：
  - `/line_replies` - 列表頁（繼承 base.html）
  - `/line_replies_edit` - 新增/編輯頁（繼承 base.html）
- 選單整合：工作紀錄區塊下，班表上方
- 權限：全部人員可見

**重要教訓**:
- 初期未使用 base.html 模板，導致樣式不一致、登入機制不相容
- 重新改寫為 Jinja2 模板格式後才符合系統規範
- **規則確立**：未來所有新頁面必須繼承 base.html，使用共用樣式與元件

**技術細節**:
- 日期時間選擇器限制不能選未來
- 門市與人員由登入機制自動帶入
- 4/1 正式啟用，從零開始記錄
- 無副檔名路由（與其它頁面一致）

### 2. 前端開發規則建立 ✅

**新增規則**（寫入白皮書第 10.5 節）：
1. 一律使用 Jinja2 模板（繼承 base.html）
2. 使用共用樣式（shared/global.css）
3. 使用共用元件（側邊欄、登入驗證）
4. 禁止獨立 HTML、禁止自定義按鈕樣式

**文件更新**:
- ERP_Whitepaper_v4.0.md 新增前端開發規則章節
- 更新版本歷史（v4.2）
- 更新 API 清單（LINE 回覆表 6組端點）
- 更新資料表清單（line_replies）

### 3. GitHub 提交記錄

| Commit | 說明 |
|--------|------|
| e4b8a0d | Add LINE Reply feature (v4.2) - line_replies table, API endpoints, UI pages |
| 5657784 | Add LINE Reply menu to base.html navigation |
| e5067e8 | Fix LINE Reply edit page auth - use currentUser instead of erp_user |
| eb94664 | Update LINE Reply pages to use base.html template with consistent styling |
| 15eba3e | Add frontend development rules to whitepaper |
| 64ea9ec | Remove .html extension from LINE Reply routes for consistency |
| 80e0cf5 | Fix remaining .html links in LINE Reply pages |

---

## 系統狀態

| 項目 | 狀態 |
|------|------|
| LINE 回覆表 | ✅ 正常運作 |
| 資料庫 | ✅ line_replies 表格已建立 |
| API | ✅ 6組端點測試通過 |
| 前端頁面 | ✅ 符合系統樣式規範 |
| 選單 | ✅ 工作紀錄區塊下可見 |
| 白皮書 | ✅ 已更新 v4.2 內容 |

---

## 重要規則記錄

**前端開發黃金法則**（2026-03-27 確立）：
> 所有新頁面必須繼承 base.html，使用共用樣式與元件。禁止建立獨立 HTML 檔案，禁止自定義按鈕樣式。

**原因**：
- 確保視覺一致性
- 自動繼承登入驗證機制
- 減少重複程式碼
- 降低維護成本

---

_記錄時間: 2026-03-27 14:39_
_記錄者: Yvonne_
