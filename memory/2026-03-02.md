# 2026-03-02 工作記錄

## TEMP 新品/新客戶回歸測試 - 最終驗證

### 核心規則（已實現）

| 條件 | 顯示於待建檔中心 |
|------|----------------|
| 未取消 + TEMP-P/TEMP-C | ✅ 必須出現 |
| 已完成（needs.status='已完成'）| ✅ 仍要出現（不影響）|
| 取消（cancelled_at not null）| ❌ 必須消失 |
| 轉正式碼（code 不再 TEMP-%）| ❌ 必須消失 |

### 已修改的 API

1. **前台**：`/api/staging/records?type=product|customer`
2. **後台**：`/api/admin/table?name=staging_records`
3. **稽核**：`/api/admin/audit/summary` (A2)

### SQL 實作（以 needs 為準）

```sql
-- 商品待建檔
SELECT DISTINCT sr.*, n.id as needs_id, n.status as needs_status
FROM needs n
JOIN staging_records sr ON sr.temp_product_id = n.product_staging_id
WHERE n.cancelled_at IS NULL
  AND n.product_code LIKE 'TEMP-P-%'
ORDER BY sr.created_at DESC;

-- 客戶待建檔
SELECT DISTINCT sr.*, n.id as needs_id, n.status as needs_status
FROM needs n
JOIN staging_records sr ON sr.temp_customer_id = n.customer_staging_id
WHERE n.cancelled_at IS NULL
  AND n.customer_code LIKE 'TEMP-C-%'
ORDER BY sr.created_at DESC;

-- A2 稽核（待建檔異常）
SELECT s.id
FROM staging_records s
JOIN needs n ON n.product_staging_id = s.temp_product_id
WHERE s.type = 'product'
  AND s.status IN ('pending', 'needs_review')
  AND n.cancelled_at IS NULL
  AND n.product_code NOT LIKE 'TEMP-P-%';
```

### 手動驗證步驟

下次遇到「新品、按已完成」的情境，執行以下檢查：

```sql
-- 1. 查詢 needs 狀態
SELECT id, product_code, product_staging_id, status, cancelled_at 
FROM needs 
WHERE id = <那筆needs_id>;
-- 預期：product_staging_id = TEMP-P-% 且 cancelled_at = NULL

-- 2. 打 API 驗證
-- 前台：/api/staging/records?type=product
-- 後台：/api/admin/table?name=staging_records
-- A2：/api/admin/audit/detail?code=A2
```

只要 DB 符合但 API 查不到，就是 API 過濾條件有 bug。

### 測試結果

| 測試項目 | 結果 |
|---------|------|
| 已完成 TEMP-P 仍可見 | ✅ PASS |
| 取消後消失 | ✅ PASS |
| 前台 API | ✅ 200 OK |
| 後台 API | ✅ 200 OK |
| A2 稽核 | ✅ 獨立運作 |

### 迴歸測試腳本

檔案：`regression_temp_flow.py`

功能：
1. 建立 TEMP-P needs（未取消）
2. 標記為「已完成」
3. 驗證三處都可見
4. 取消 needs
5. 驗證三處都不可見
6. 清理測試資料

### 當前資料庫狀態

```sql
-- TEMP-P 未取消：0 筆（全部已取消或無資料）
-- TEMP-C 未取消：2 筆（14854, 14852，皆已完成但仍在待建檔中心）
```

### 結論

✅ **以 needs 為準的顯示邏輯已正確實現**
- 不限制 `needs.status`（pending/已完成/已處理 都顯示）
- 不限制 `staging_records.status`（pending/resolved 都顯示）
- 只檢查 `cancelled_at IS NULL` + `code LIKE 'TEMP-%'`
